import { TabPlaceholder } from '@/components/tab-placeholder';

export default function HealthPage() {
  return (
    <TabPlaceholder
      title="Health"
      note="Not built yet. Health is a temperature log: it records readings and nothing else. It will never tell you whether a reading is high, normal or concerning."
    />
  );
}
