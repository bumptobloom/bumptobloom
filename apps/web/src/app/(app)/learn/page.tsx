import { TabPlaceholder } from '@/components/tab-placeholder';

export default function LearnPage() {
  return (
    <TabPlaceholder
      title="Learn"
      note="Not built yet. The articles table is empty until the content reuse question is answered, so there is nothing to read here even once the screen exists."
    />
  );
}
